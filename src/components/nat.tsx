import React from "react";
import IconComponent from "../IconComponent";

/**
 * icon name: nat
 *
 * categroies: communication
 *
 * tags: communication, nat
 *
 * popularity: 1347
 */
const Nat = (props: React.HTMLAttributes<HTMLDivElement>) => (
  <IconComponent iconName="nat" {...props} />
);

export default Nat;
