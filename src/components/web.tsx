import React from "react";
import IconComponent from "../IconComponent";

/**
 * icon name: web
 *
 * categroies: av
 *
 * tags: browser, internet, page, screen, site, web, website, www
 *
 * popularity: 16755
 */
const Web = (props: React.HTMLAttributes<HTMLDivElement>) => (
  <IconComponent iconName="web" {...props} />
);

export default Web;
