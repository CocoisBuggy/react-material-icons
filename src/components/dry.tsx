import React from "react";
import IconComponent from "../IconComponent";

/**
 * icon name: dry
 *
 * categroies: places
 *
 * tags: air, bathroom, dry, dryer, fingers, gesture, hand, wc
 *
 * popularity: 1512
 */
const Dry = (props: React.HTMLAttributes<HTMLDivElement>) => (
  <IconComponent iconName="dry" {...props} />
);

export default Dry;
