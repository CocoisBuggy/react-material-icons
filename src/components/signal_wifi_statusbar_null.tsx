import React from "react";
import IconComponent from "../IconComponent";

/**
 * icon name: signal_wifi_statusbar_null
 *
 * categroies: device
 *
 * tags: cell, cellular, data, internet, mobile, network, null, phone, signal, speed, statusbar, wifi, wireless
 *
 * popularity: 1964
 */
const SignalWifiStatusbarNull = (
  props: React.HTMLAttributes<HTMLDivElement>,
) => <IconComponent iconName="signal_wifi_statusbar_null" {...props} />;

export default SignalWifiStatusbarNull;
