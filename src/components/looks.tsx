import React from "react";
import IconComponent from "../IconComponent";

/**
 * icon name: looks
 *
 * categroies: image
 *
 * tags: circle, half, looks, rainbow
 *
 * popularity: 2474
 */
const Looks = (props: React.HTMLAttributes<HTMLDivElement>) => (
  <IconComponent iconName="looks" {...props} />
);

export default Looks;
