import React from "react";
import IconComponent from "../IconComponent";

/**
 * icon name: format_textdirection_r_to_l
 *
 * categroies: editor
 *
 * tags: align, alignment, doc, edit, editing, editor, format, rtl, sheet, spreadsheet, text, textdirection, type, writing
 *
 * popularity: 780
 */
const FormatTextdirectionRToL = (
  props: React.HTMLAttributes<HTMLDivElement>,
) => <IconComponent iconName="format_textdirection_r_to_l" {...props} />;

export default FormatTextdirectionRToL;
