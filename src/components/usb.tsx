import React from "react";
import IconComponent from "../IconComponent";

/**
 * icon name: usb
 *
 * categroies: device
 *
 * tags: cable, connection, device, usb, wire
 *
 * popularity: 4534
 */
const Usb = (props: React.HTMLAttributes<HTMLDivElement>) => (
  <IconComponent iconName="usb" {...props} />
);

export default Usb;
