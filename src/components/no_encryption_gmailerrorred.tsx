import React from "react";
import IconComponent from "../IconComponent";

/**
 * icon name: no_encryption_gmailerrorred
 *
 * categroies: notification
 *
 * tags: disabled, enabled, encryption, error, gmail, lock, locked, no, off, on, slash
 *
 * popularity: 1727
 */
const NoEncryptionGmailerrorred = (
  props: React.HTMLAttributes<HTMLDivElement>,
) => <IconComponent iconName="no_encryption_gmailerrorred" {...props} />;

export default NoEncryptionGmailerrorred;
