import React from "react";
import IconComponent from "../IconComponent";

/**
 * icon name: format_textdirection_l_to_r
 *
 * categroies: editor
 *
 * tags: align, alignment, doc, edit, editing, editor, format, ltr, sheet, spreadsheet, text, textdirection, type, writing
 *
 * popularity: 829
 */
const FormatTextdirectionLToR = (
  props: React.HTMLAttributes<HTMLDivElement>,
) => <IconComponent iconName="format_textdirection_l_to_r" {...props} />;

export default FormatTextdirectionLToR;
