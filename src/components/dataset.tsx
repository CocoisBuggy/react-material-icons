import React from "react";
import IconComponent from "../IconComponent";

/**
 * icon name: dataset
 *
 * categroies: device
 *
 * tags:
 *
 * popularity: 1530
 */
const Dataset = (props: React.HTMLAttributes<HTMLDivElement>) => (
  <IconComponent iconName="dataset" {...props} />
);

export default Dataset;
