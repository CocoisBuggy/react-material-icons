import React from "react";
import IconComponent from "../IconComponent";

/**
 * icon name: settings_input_composite
 *
 * categroies: action
 *
 * tags: component, composite, connection, connectivity, input, plug, points, settings
 *
 * popularity: 4909
 */
const SettingsInputComposite = (
  props: React.HTMLAttributes<HTMLDivElement>,
) => <IconComponent iconName="settings_input_composite" {...props} />;

export default SettingsInputComposite;
