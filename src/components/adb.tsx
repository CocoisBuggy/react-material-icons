import React from "react";
import IconComponent from "../IconComponent";

/**
 * icon name: adb
 *
 * categroies: notification
 *
 * tags: adb, android, bridge, debug
 *
 * popularity: 4486
 */
const Adb = (props: React.HTMLAttributes<HTMLDivElement>) => (
  <IconComponent iconName="adb" {...props} />
);

export default Adb;
